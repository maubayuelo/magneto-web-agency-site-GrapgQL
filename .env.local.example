# Example environment variables for local development
# Copy this file to `.env.local` and replace the placeholders with real values.

# If you need to disable TLS checks in local dev (not recommended), set to 0
NODE_TLS_REJECT_UNAUTHORIZED=0

# Mailchimp API Key and List ID - keep these secret in your local .env.local
MAILCHIMP_API_KEY=your_mailchimp_api_key_here
MAILCHIMP_LIST_ID=your_mailchimp_list_id_here

# Optionally add other keys used by the project below:
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id
# OTHER_SECRET=secret_value
# WARNING: Do NOT commit this file. Keep your secrets local only.
NODE_TLS_REJECT_UNAUTHORIZED=0
MAILCHIMP_API_KEY=your_mailchimp_api_key_here
MAILCHIMP_SERVER_PREFIX=your_mailchimp_server_prefix_here
MAILCHIMP_LIST_ID=your_mailchimp_list_id_here
NEXT_PUBLIC_GA_ID=your_ga_id_here
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your_turnstile_site_key_here
# .env.local (do NOT commit)
SMTP_HOST=your_smtp_host_here
SMTP_USER=your_smtp_user_here
SMTP_PASS=your_smtp_password_here
SMTP_PORT=your_smtp_port_here
EMAIL_FROM="Magneto <no-reply@yourdomain.com>"
CONTACT_RECIPIENT=your_contact_recipient_email_here